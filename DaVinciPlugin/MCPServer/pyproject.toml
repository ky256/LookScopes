[project]
name = "davinci-mcp"
version = "0.1.0"
description = "DaVinci Resolve AI Color Grading MCP Server"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "numpy>=1.24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
